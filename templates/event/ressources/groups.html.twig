{% extends 'event/ressources/base.html.twig' %}

{% block resource_container %}
    {% if event.groups|length > 0 %}
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3>Liste des groupes</h3>
            <a class="btn btn-sm btn-primary" href="{{ path('event_resources_groups_new', {id: event.id}) }}"><i class="fa fa-plus mr-2"></i>Créer un groupe</a>
        </div>
        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th>Groupe d'équité</th>
                <th>Limite de temps</th>
                <th>Nombre de ressources</th>
                <th style="text-transform: capitalize;">écart type<i class="fa fa-sm fa-question-circle ml-2" title="Calcul l'écart type des ressources du groupe d'équité. Un faible écart type est signe d'équité dans le groupe."></i></th>
            </tr>
            </thead>
            <tbody>
            {% for group in event.groups %}
                <tr>
                    <td><a href="{{ path('event_resources_groups_edit', {id: event.id, group_id: group.id}) }}">{{ group.name }}</a></td>
                    <td>{{ group.workingLimit ? group.workingLimit|date('H\\hi') : '-' }}</td>
                    <td>{{ group.availabilities|length }}</td>
                    <td><span class="badge badge-success">1h</span></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    {% else %}
        <div class="mr-5 ml-5 mt-3 text-center">
            <i class="fa fa-equity fa-5x mb-4"></i>
            <h3 class="mb-4">Aucun groupe d'équité pour le moment</h3>
            <p>Les groupes d'équité permettent de définir des groupes de ressources qui doivent avoir une charge de travail équivalente.</p>
            <p>Une ressource peut alors appartenir au plus a un seul groupe d'équité.</p>
            <p>Il est également possible de fixer une limite de charge de travail pour un groupe d'équité.</p>
            <a class="btn btn-primary mt-2" href="{{ path('event_resources_groups_new', {id: event.id}) }}"><i class="fa fa-plus mr-2"></i>Créer un groupe d'équité</a>
        </div>
    {% endif %}
{% endblock %}

